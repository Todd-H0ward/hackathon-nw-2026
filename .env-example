# Generic backend (src/shared/api/api.ts)
VITE_API_URL=http://localhost:8000/api

# XenoChoice Sandbox API.
# Absolute URL — works in dev and in a production build.
VITE_XENOCHOICE_API_URL=/api/v2
# Alternative for dev: route through the Vite proxy to avoid mixed content
# when the app itself is served over https.
# VITE_XENOCHOICE_API_URL=/api/v2
